import { ZeropaddingPipe } from './zeropadding.pipe';

describe('ZeropaddingPipe', () => {
  it('create an instance', () => {
    const pipe = new ZeropaddingPipe();
    expect(pipe).toBeTruthy();
  });
});
